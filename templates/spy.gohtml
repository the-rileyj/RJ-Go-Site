<!DOCTYPE html>
<html>
    {{template "defaulthead"}}
    <body>
        {{template "header"}}
        <div class="container-fluid">
            <div class="row no-gutters" >

            </div>
        </div>
        {{template "footer"}}
        <!--div.super-spacer
        <script type="text/javascript">
            function getWS() {
                var socket = new WebSocket("ws://localhost:9000/spy");
                return socket;
            }
        </script>-->
    </body>
</html>